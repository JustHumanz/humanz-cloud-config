horizon_helm_values:
  conf:
    horizon:
      branding:
        logo: "{{ lookup('file', inventory_dir ~ '/../assets/logo.png') | b64encode }}"
        logo_splash: "{{ lookup('file', inventory_dir ~ '/../assets/logo-splash.png') | b64encode }}"
        favicon: "{{ lookup('file', inventory_dir ~ '/../assets/openstack-icon.svg') | b64encode }}"
  manifests:
    configmap_logo: true